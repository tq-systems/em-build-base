variables:
  BASE_DOCKER_TAG: 'v4.0.0'

.image-ubuntu:
  image: ${BASE_REGISTRY}/ubuntu:${BASE_DOCKER_TAG}

.image-test:
  image: ${BASE_REGISTRY}/test:${BASE_DOCKER_TAG}

.image-docker:
  image: ${BASE_REGISTRY}/docker:${BASE_DOCKER_TAG}
